"""Reusable condition / perturbation embedding packages.

Subdirectories:
    ``genepert/`` — gene-level perturb conditioning (primary implementation).

    ``chempert/`` — chemical perturbation stubs + integration notes (vectors still
        flow via ``genepert.chem_embedding_hook`` until split further).
"""
